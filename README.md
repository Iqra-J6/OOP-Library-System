# OOP-Library-System
The system uses object-oriented design and the command pattern to manage library operations, supports data persistence through text files, and provides both a CLI and GUI interface while enforcing real-world library rules.
